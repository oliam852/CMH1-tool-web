<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CMH-1 Pro - Ultimate Email Headers Processor </title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=JetBrains+Mono:wght@300;400;500;600&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <style>
        :root {
            --bg-primary: #1a1b26;
            --bg-secondary: #1a1b26;
            --bg-tertiary: #1a1b26;
            --accent-purple: #a855f7;
            --accent-blue: #3b82f6;
            --accent-cyan: #06b6d4;
            --text-primary: #e2e8f0;
            --text-secondary: #94a3b8;
            --border-color: rgba(168, 85, 247, 0.2);
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --gradient-main: linear-gradient(135deg, #a855f7 0%, #3b82f6 100%);
            --gradient-card: linear-gradient(135deg, rgba(168, 85, 247, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.3);
            --shadow-lg: 0 10px 40px rgba(168, 85, 247, 0.3);
            --shadow-glow: 0 0 30px rgba(168, 85, 247, 0.5);
        }

        [data-theme="light"] {
            --bg-primary: #f8fafc;
            --bg-secondary: #ffffff;
            --bg-tertiary: #f1f5f9;
            --text-primary: #0f172a;
            --text-secondary: #475569;
            --border-color: rgba(168, 85, 247, 0.3);
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 40px rgba(168, 85, 247, 0.2);
            --shadow-glow: 0 0 30px rgba(168, 85, 247, 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'JetBrains Mono', monospace;
            background: var(--bg-primary);
            color: var(--text-primary);
            transition: background 0.4s cubic-bezier(0.4, 0, 0.2, 1), color 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            overflow-x: hidden;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 30% 50%, rgba(168, 85, 247, 0.08) 0%, transparent 50%),
                        radial-gradient(circle at 70% 80%, rgba(59, 130, 246, 0.08) 0%, transparent 50%);
            animation: gradientMove 15s ease-in-out infinite;
            pointer-events: none;
            z-index: 0;
        }

        @keyframes gradientMove {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            50% { transform: translate(-5%, -5%) rotate(5deg); }
        }

        .container {
            max-width: 1600px;
            margin: 0 auto;
            padding: 2rem;
            position: relative;
            z-index: 1;
        }

        header {
            text-align: center;
            margin-bottom: 3rem;
            animation: slideDown 0.8s cubic-bezier(0.22, 1, 0.36, 1);
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .logo-container {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .logo {
            width: 80px;
            height: 80px;
            background: var(--gradient-main);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Orbitron', sans-serif;
            font-size: 2rem;
            font-weight: 900;
            color: white;
            box-shadow: var(--shadow-glow);
            animation: pulse 3s ease-in-out infinite;
            position: relative;
            overflow: hidden;
        }

        .logo::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.3) 50%, transparent 70%);
            animation: shine 3s infinite;
        }

        @keyframes shine {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 3.5rem;
            font-weight: 900;
            background: var(--gradient-main);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
            letter-spacing: -2px;
        }

        .subtitle {
            font-size: 1.1rem;
            color: var(--text-secondary);
            font-weight: 400;
        }

        .theme-toggle {
            position: fixed;
            top: 2rem;
            right: 2rem;
            z-index: 1000;
            background: var(--bg-secondary);
            border: 2px solid var(--border-color);
            border-radius: 50px;
            padding: 0.5rem 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-sm);
        }

        .theme-toggle:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .theme-toggle svg {
            width: 20px;
            height: 20px;
            fill: var(--text-primary);
            transition: transform 0.3s ease;
        }

        .theme-toggle:hover svg {
            transform: rotate(20deg);
        }

        .main-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .card {
            background: var(--bg-secondary);
            border: 2px solid var(--border-color);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: var(--shadow-sm);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            animation: fadeIn 0.8s ease-out backwards;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--gradient-main);
        }

        .card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-lg);
            border-color: var(--accent-purple);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card:nth-child(1) { animation-delay: 0.1s; }
        .card:nth-child(2) { animation-delay: 0.2s; }
        .card:nth-child(3) { animation-delay: 0.3s; }
        .card:nth-child(4) { animation-delay: 0.4s; }

        .card-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .card-icon {
            width: 45px;
            height: 45px;
            background: var(--gradient-card);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .card-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .drop-zone {
            border: 3px dashed var(--border-color);
            border-radius: 15px;
            padding: 3rem 2rem;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
            background: var(--bg-tertiary);
            position: relative;
        }

        .drop-zone:hover,
        .drop-zone.dragover {
            border-color: var(--accent-purple);
            background: var(--gradient-card);
            transform: scale(1.02);
        }

        .drop-zone-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            opacity: 0.7;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .input-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--text-secondary);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        input[type="text"],
        input[type="file"],
        textarea,
        select {
            width: 100%;
            padding: 0.8rem 1.2rem;
            background: var(--bg-tertiary);
            border: 2px solid var(--border-color);
            border-radius: 10px;
            color: var(--text-primary);
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.95rem;
            transition: all 0.3s ease;
        }

        input[type="text"]:focus,
        textarea:focus,
        select:focus {
            outline: none;
            border-color: var(--accent-purple);
            box-shadow: 0 0 0 4px rgba(168, 85, 247, 0.1);
        }

        textarea {
            min-height: 150px;
            resize: vertical;
            font-size: 0.85rem;
        }

        .checkbox-group {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
            margin: 1rem 0;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.8rem;
            background: var(--bg-tertiary);
            border-radius: 10px;
            border: 2px solid transparent;
            transition: all 0.2s ease;
            cursor: pointer;
        }

        .checkbox-item:hover {
            border-color: var(--accent-purple);
            background: var(--gradient-card);
        }

        input[type="checkbox"] {
            width: 20px;
            height: 20px;
            cursor: pointer;
            accent-color: var(--accent-purple);
        }

        .checkbox-label {
            cursor: pointer;
            font-size: 0.9rem;
            user-select: none;
        }

        .btn {
            padding: 1rem 2.5rem;
            font-family: 'Orbitron', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            background: var(--gradient-main);
            color: white;
            box-shadow: var(--shadow-lg);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-glow);
        }

        .btn-secondary {
            background: var(--bg-tertiary);
            color: var(--text-primary);
            border: 2px solid var(--border-color);
        }

        .btn-secondary:hover {
            border-color: var(--accent-blue);
            background: var(--gradient-card);
        }

        .btn-group {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-top: 1.5rem;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin: 2rem 0;
        }

        .stat-card {
            background: var(--gradient-card);
            border: 2px solid var(--border-color);
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-sm);
        }

        .stat-value {
            font-family: 'Orbitron', sans-serif;
            font-size: 2.5rem;
            font-weight: 900;
            background: var(--gradient-main);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-label {
            font-size: 0.85rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 0.5rem;
        }

        .comparison-view {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .comparison-panel {
            background: var(--bg-tertiary);
            border-radius: 15px;
            padding: 1.5rem;
            border: 2px solid var(--border-color);
        }

        .comparison-panel h3 {
            font-family: 'Orbitron', sans-serif;
            margin-bottom: 1rem;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .comparison-panel pre {
            background: var(--bg-primary);
            padding: 1rem;
            border-radius: 10px;
            overflow-x: auto;
            font-size: 0.85rem;
            line-height: 1.6;
        }

        .notification {
            position: fixed;
            top: 6rem;
            right: 2rem;
            background: var(--bg-secondary);
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 1rem 1.5rem;
            box-shadow: var(--shadow-lg);
            z-index: 1000;
            animation: slideInRight 0.3s ease-out;
            max-width: 400px;
        }

        @keyframes slideInRight {
            from {
                transform: translateX(500px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .notification.success { border-left: 5px solid var(--success); }
        .notification.warning { border-left: 5px solid var(--warning); }
        .notification.error { border-left: 5px solid var(--danger); }

        .regex-builder {
            background: var(--bg-tertiary);
            border-radius: 15px;
            padding: 1.5rem;
            margin-top: 1rem;
        }

        .regex-pattern {
            font-family: 'JetBrains Mono', monospace;
            background: var(--bg-primary);
            padding: 1rem;
            border-radius: 10px;
            border: 2px solid var(--accent-cyan);
            font-size: 0.9rem;
            color: var(--accent-cyan);
            margin-top: 1rem;
        }

        .file-list {
            max-height: 300px;
            overflow-y: auto;
            margin-top: 1rem;
        }

        .file-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.8rem;
            background: var(--bg-tertiary);
            border-radius: 8px;
            margin-bottom: 0.5rem;
            border: 2px solid transparent;
            transition: all 0.2s ease;
        }

        .file-item:hover {
            border-color: var(--accent-purple);
        }

        .file-info {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .file-icon {
            font-size: 1.5rem;
        }

        .remove-file {
            background: var(--danger);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .remove-file:hover {
            transform: scale(1.05);
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background: var(--bg-tertiary);
            border-radius: 10px;
            overflow: hidden;
            margin: 1rem 0;
        }

        .progress-fill {
            height: 100%;
            background: var(--gradient-main);
            transition: width 0.3s ease;
            animation: progressShine 1.5s infinite;
        }

        @keyframes progressShine {
            0% { opacity: 0.8; }
            50% { opacity: 1; }
            100% { opacity: 0.8; }
        }

        .footer {
            text-align: center;
            padding: 2rem;
            margin-top: 4rem;
            border-top: 2px solid var(--border-color);
        }

        .footer a {
            color: var(--accent-purple);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .footer a:hover {
            color: var(--accent-blue);
        }

        .tab-container {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
            border-bottom: 2px solid var(--border-color);
        }

        .tab {
            padding: 1rem 2rem;
            background: transparent;
            border: none;
            color: var(--text-secondary);
            cursor: pointer;
            font-family: 'Orbitron', sans-serif;
            font-weight: 600;
            position: relative;
            transition: all 0.3s ease;
        }

        .tab:hover {
            color: var(--text-primary);
        }

        .tab.active {
            color: var(--accent-purple);
        }

        .tab.active::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--gradient-main);
            animation: slideIn 0.3s ease-out;
        }

        @keyframes slideIn {
            from {
                transform: scaleX(0);
            }
            to {
                transform: scaleX(1);
            }
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
            animation: fadeIn 0.3s ease-out;
        }

        @media (max-width: 1024px) {
            .main-grid {
                grid-template-columns: 1fr;
            }
            
            .comparison-view {
                grid-template-columns: 1fr;
            }

            h1 {
                font-size: 2.5rem;
            }
        }

        .hidden {
            display: none !important;
        }

        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-tertiary);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--gradient-main);
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--accent-purple);
        }

        .fix-badge {
            background: var(--success);
            color: black;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.7rem;
            font-weight: 700;
            margin-left: 0.5rem;
            animation: pulse 2s infinite;
        }
    </style>
</head>
<body data-theme="dark">
    <div class="theme-toggle" onclick="toggleTheme()">
        <svg id="themeIcon" viewBox="0 0 24 24">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
        <span id="themeText">Dark</span>
    </div>

    <div class="container">
        <header>
            <div class="logo-container">
                <div class="logo">C1</div>
            </div>
            <h1>CMH-1 PRO <span class="fix-badge">Headers Processing Tool</span></h1>
            <p class="subtitle">Ultimate Email Headers Processing Tool</p>
        </header>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-value" id="totalProcessed">0</div>
                <div class="stat-label">Processed</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="totalFiles">0</div>
                <div class="stat-label">Files Loaded</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="successRate">100%</div>
                <div class="stat-label">Success Rate</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="processingTime">0s</div>
                <div class="stat-label">Avg Time</div>
            </div>
        </div>

        <div class="main-grid">
            <div class="card">
                <div class="card-header">
                    <div class="card-icon">📥</div>
                    <h2 class="card-title">Input Headers</h2>
                </div>

                <div class="tab-container">
                    <button class="tab active" onclick="switchTab('paste')">Paste</button>
                    <button class="tab" onclick="switchTab('upload')">Upload</button>
                </div>

                <div id="paste-tab" class="tab-content active">
                    <textarea id="inputHeaders" placeholder="Paste email headers here..."></textarea>
                </div>

                <div id="upload-tab" class="tab-content">
                    <div class="drop-zone" id="dropZone" onclick="document.getElementById('fileInput').click()">
                        <div class="drop-zone-icon">📄</div>
                        <h3>Drop files here or click to browse</h3>
                        <p style="color: var(--text-secondary); margin-top: 0.5rem;">Supports .eml, .txt, .msg files</p>
                    </div>
                    <input type="file" id="fileInput" accept=".eml,.txt,.msg" multiple style="display: none;">
                    <div class="file-list" id="fileList"></div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <div class="card-icon">⚙️</div>
                    <h2 class="card-title">Processing Options</h2>
                </div>

                <div class="input-group">
                    <label>Domain Replacement</label>
                    <input type="text" id="domainReplacement" placeholder="Leave empty to use [P_FROM]">
                </div>

                <div class="input-group">
                    <label>Custom Headers (Key:Value, one per line)</label>
                    <textarea id="customHeaders" placeholder="X-Custom-Header: Value&#10;X-Another: AnotherValue" style="min-height: 80px;"></textarea>
                </div>

                <div class="input-group">
                    <label>Message-ID Tag</label>
                    <input type="text" id="messageIdTag" placeholder="Custom tag for Message-ID (e.g., [EID])">
                </div>

                <div class="checkbox-group">
                    <div class="checkbox-item">
                        <input type="checkbox" id="extractBodyCheckbox" checked>
                        <label class="checkbox-label" for="extractBodyCheckbox">Extract Header + Body</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="replaceDateCheckbox" checked>
                        <label class="checkbox-label" for="replaceDateCheckbox">Replace Date with [DATE]</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="replaceToCheckbox" checked>
                        <label class="checkbox-label" for="replaceToCheckbox">Replace To with [*to]</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="keepReceivedCheckbox" checked>
                        <label class="checkbox-label" for="keepReceivedCheckbox">Keep "Received" headers</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="keepReplyToCheckbox" checked>
                        <label class="checkbox-label" for="keepReplyToCheckbox">Keep "Reply-To" headers</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="addCcCheckbox" checked>
                        <label class="checkbox-label" for="addCcCheckbox">Add "Cc: [*to]" if missing</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="removeDuplicatesCheckbox">
                        <label class="checkbox-label" for="removeDuplicatesCheckbox">Remove Duplicates</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="nameBySubjectCheckbox">
                        <label class="checkbox-label" for="nameBySubjectCheckbox">Name files by Subject</label>
                    </div>
                    <div class="checkbox-item" style="border: 2px solid var(--danger);">
                        <input type="checkbox" id="removeSecurityHeadersCheckbox" checked>
                        <label class="checkbox-label" for="removeSecurityHeadersCheckbox">🗑️ Remove Security Headers</label>
                    </div>
                </div>

                <div class="btn-group">
                    <button class="btn btn-primary" onclick="processHeaders()">
                        <span style="position: relative; z-index: 1;">🚀 Process Headers</span>
                    </button>
                    <button class="btn btn-secondary" onclick="clearAll()">
                        <span style="position: relative; z-index: 1;">🗑️ Clear</span>
                    </button>
                </div>
            </div>
        </div>

        <div class="card" style="animation-delay: 0.6s;">
            <div class="card-header">
                <div class="card-icon">📤</div>
                <h2 class="card-title">Processed Output</h2>
            </div>

            <div class="input-group">
                <label>Export Format</label>
                <select id="exportFormat">
                    <option value="txt">Single File (.txt)</option>
                    <option value="sep">Merged with __SEP__ (.txt)</option>
                    <option value="zip">Separate Files (.zip)</option>
                    <option value="json">JSON (.json)</option>
                    <option value="csv">CSV (.csv)</option>
                </select>
            </div>

            <textarea id="outputHeaders" readonly placeholder="Processed headers will appear here..."></textarea>

            <div class="btn-group">
                <button class="btn btn-primary" onclick="downloadOutput()">
                    <span style="position: relative; z-index: 1;">💾 Download</span>
                </button>
                <button class="btn btn-secondary" onclick="copyToClipboard()">
                    <span style="position: relative; z-index: 1;">📋 Copy</span>
                </button>
                <button class="btn btn-secondary" onclick="showComparison()">
                    <span style="position: relative; z-index: 1;">🔍 Compare</span>
                </button>
            </div>
        </div>

        <div id="comparisonSection" class="hidden">
            <div class="card" style="animation-delay: 0.7s;">
                <div class="card-header">
                    <div class="card-icon">⚖️</div>
                    <h2 class="card-title">Before & After Comparison</h2>
                </div>
                <div class="comparison-view">
                    <div class="comparison-panel">
                        <h3>⬅️ Original</h3>
                        <pre id="comparisonBefore"></pre>
                    </div>
                    <div class="comparison-panel">
                        <h3>➡️ Processed</h3>
                        <pre id="comparisonAfter"></pre>
                    </div>
                </div>
            </div>
        </div>

        <footer class="footer">
            <p>Built with 💜 by <a href="https://t.me/ayoubrhattoy" target="_blank">@ayoubrhattoy</a></p>
            <p style="margin-top: 0.5rem; color: var(--text-secondary); font-size: 0.9rem;">CMH-1 Pro v2.0 FIXED | Ultimate Email Headers Processor</p>
        </footer>
    </div>

    <script>
        // Global State
        let uploadedFiles = [];
        let uploadedFilesContent = []; // ✅ NEW: Store file contents
        let processedFilesData = []; // ✅ NEW: Store individually processed files for ZIP
        let processedCount = 0;
        let totalProcessingTime = 0;
        let originalHeaders = '';
        let processedHeaders = '';

        // Theme Toggle
        function toggleTheme() {
            const body = document.body;
            const currentTheme = body.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            body.setAttribute('data-theme', newTheme);
            
            const themeText = document.getElementById('themeText');
            const themeIcon = document.getElementById('themeIcon');
            
            if (newTheme === 'light') {
                themeText.textContent = 'Light';
                themeIcon.innerHTML = '<path d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>';
            } else {
                themeText.textContent = 'Dark';
                themeIcon.innerHTML = '<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>';
            }
        }

        // Tab Switching
        function switchTab(tabName) {
            const tabs = document.querySelectorAll('.tab');
            const tabContents = document.querySelectorAll('.tab-content');
            
            tabs.forEach(tab => tab.classList.remove('active'));
            tabContents.forEach(content => content.classList.remove('active'));
            
            event.target.classList.add('active');
            document.getElementById(tabName + '-tab').classList.add('active');
        }

        // Drag and Drop
        const dropZone = document.getElementById('dropZone');
        
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
            dropZone.addEventListener(eventName, preventDefaults, false);
        });

        function preventDefaults(e) {
            e.preventDefault();
            e.stopPropagation();
        }

        ['dragenter', 'dragover'].forEach(eventName => {
            dropZone.addEventListener(eventName, () => dropZone.classList.add('dragover'), false);
        });

        ['dragleave', 'drop'].forEach(eventName => {
            dropZone.addEventListener(eventName, () => dropZone.classList.remove('dragover'), false);
        });

        dropZone.addEventListener('drop', handleDrop, false);

        function handleDrop(e) {
            const dt = e.dataTransfer;
            const files = dt.files;
            handleFiles(files);
        }

        document.getElementById('fileInput').addEventListener('change', function(e) {
            handleFiles(e.target.files);
        });

        // ✅ FIXED: Now reads file contents
        async function handleFiles(files) {
            uploadedFiles = Array.from(files);
            uploadedFilesContent = [];
            
            for (let file of uploadedFiles) {
                try {
                    const content = await readFileContent(file);
                    uploadedFilesContent.push({
                        name: file.name,
                        content: content,
                        size: file.size
                    });
                } catch (error) {
                    console.error('Error reading file:', file.name, error);
                    showNotification(`Error reading ${file.name}`, 'error');
                }
            }
            
            updateFileList();
            updateStats();
            showNotification(`${uploadedFiles.length} file(s) loaded successfully!`, 'success');
        }

        // ✅ NEW: Function to read file content
        function readFileContent(file) {
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.onload = (e) => resolve(e.target.result);
                reader.onerror = (e) => reject(e);
                reader.readAsText(file);
            });
        }

        function updateFileList() {
            const fileList = document.getElementById('fileList');
            fileList.innerHTML = '';
            
            uploadedFiles.forEach((file, index) => {
                const fileItem = document.createElement('div');
                fileItem.className = 'file-item';
                fileItem.innerHTML = `
                    <div class="file-info">
                        <span class="file-icon">📄</span>
                        <span>${file.name}</span>
                        <span style="color: var(--text-secondary); font-size: 0.85rem;">(${formatFileSize(file.size)})</span>
                    </div>
                    <button class="remove-file" onclick="removeFile(${index})">×</button>
                `;
                fileList.appendChild(fileItem);
            });
        }

        function removeFile(index) {
            uploadedFiles.splice(index, 1);
            uploadedFilesContent.splice(index, 1);
            updateFileList();
            updateStats();
        }

        function formatFileSize(bytes) {
            if (bytes < 1024) return bytes + ' B';
            if (bytes < 1048576) return (bytes / 1024).toFixed(1) + ' KB';
            return (bytes / 1048576).toFixed(1) + ' MB';
        }

        // ✅ FIXED: Process Headers now handles uploaded files
        async function processHeaders() {
            const startTime = performance.now();
            let input = document.getElementById('inputHeaders').value;
            
            // ✅ NEW: Check for uploaded files
            if (!input && uploadedFilesContent.length === 0) {
                showNotification('Please add headers or upload files!', 'warning');
                return;
            }

            // Reset processed files data
            processedFilesData = [];

            // ✅ NEW: Process uploaded files if available
            if (uploadedFilesContent.length > 0) {
                let allProcessed = [];
                for (let fileData of uploadedFilesContent) {
                    const processed = processHeadersText(fileData.content);
                    allProcessed.push(`\n=== ${fileData.name} ===\n${processed}`);
                    
                    // Store individual processed file
                    processedFilesData.push({
                        name: fileData.name,
                        content: processed
                    });
                }
                processedHeaders = allProcessed.join('\n\n');
                document.getElementById('outputHeaders').value = processedHeaders;
            } else {
                // Process textarea input
                originalHeaders = input;
                processedHeaders = processHeadersText(input);
                document.getElementById('outputHeaders').value = processedHeaders;
                
                // Store as single file
                processedFilesData.push({
                    name: 'processed_headers.txt',
                    content: processedHeaders
                });
            }
            
            // Update stats
            const endTime = performance.now();
            const processingTime = ((endTime - startTime) / 1000).toFixed(2);
            processedCount++;
            totalProcessingTime += parseFloat(processingTime);
            updateStats();
            
            showNotification('Headers processed successfully! 🎉', 'success');
        }

        // ✅ NEW: Separate function for processing header text
        function processHeadersText(input) {
            let lines = input.split('\n');
            
            // Get processing options
            const domainReplacement = document.getElementById('domainReplacement').value;
            const messageIdTag = document.getElementById('messageIdTag').value;
            const replaceDate = document.getElementById('replaceDateCheckbox').checked;
            const replaceTo = document.getElementById('replaceToCheckbox').checked;
            const keepReceived = document.getElementById('keepReceivedCheckbox').checked;
            const keepReplyTo = document.getElementById('keepReplyToCheckbox').checked;
            const addCc = document.getElementById('addCcCheckbox').checked;
            const extractBody = document.getElementById('extractBodyCheckbox').checked;
            const customHeaders = document.getElementById('customHeaders').value;
            const removeSecurityHeaders = document.getElementById('removeSecurityHeadersCheckbox').checked;
            
            // Parse headers and body separately
            let headers = [];
            let currentHeader = null;
            let bodyStartIndex = -1;
            let bodyContent = '';
            
            for (let i = 0; i < lines.length; i++) {
                let line = lines[i];
                
                if (bodyStartIndex === -1) {
                    if (line.match(/^[A-Z][A-Za-z0-9-]*:\s*/)) {
                        if (currentHeader) {
                            headers.push(currentHeader);
                        }
                        const colonIndex = line.indexOf(':');
                        currentHeader = { 
                            name: line.substring(0, colonIndex).trim(), 
                            fullLine: line 
                        };
                    } else if (currentHeader && line.match(/^\s+/)) {
                        currentHeader.fullLine += '\n' + line;
                    } else if (line.trim() === '') {
                        if (currentHeader) {
                            headers.push(currentHeader);
                            currentHeader = null;
                        }
                        if (i + 1 < lines.length && !lines[i + 1].match(/^[A-Z][A-Za-z0-9-]*:\s*/)) {
                            bodyStartIndex = i + 1;
                        }
                    }
                } else {
                    bodyContent += line + '\n';
                }
            }
            
            if (currentHeader && bodyStartIndex === -1) {
                headers.push(currentHeader);
            }
            
            // Categorize and process headers
            let receivedHeaders = [];
            let senderHeader = null;
            let otherHeaders = [];
            let securityHeaderNames = removeSecurityHeaders ? [
                'Delivered-To', 'X-Received', 'X-Google-Smtp-Source', 'DKIM-Signature',
                'Received-SPF', 'Return-Path', 'Authentication-Results', 'X-Google-DKIM-Signature',
                'DMARC-Filter'
            ] : [];
            
            for (let header of headers) {
                let line = header.fullLine;
                
                if (removeSecurityHeaders) {
                    if (securityHeaderNames.includes(header.name) || 
                        header.name.startsWith('ARC-') || 
                        header.name.startsWith('X-Spam')) {
                        continue;
                    }
                    
                    line = line.replace(/^\s+(dkim|spf|dmarc|bh|fh|[bg]|[hb]|dara|d=google\.com).*$/gm, '');
                    line = line.replace(/^\s+[a-zA-Z0-9+/=]{40,}.*$/gm, '');
                    line = line.trim().split('\n').filter(l => l.trim()).join('\n');
                }
                
                if (header.name === 'Received') {
                    if (keepReceived) {
                        receivedHeaders.push(line);
                    }
                } else if (header.name === 'Sender') {
                    senderHeader = line;
                } else if (header.name === 'Reply-To') {
                    if (keepReplyTo) {
                        otherHeaders.push(line);
                    }
                } else {
                    if (header.name === 'From') {
                        if (domainReplacement) {
                            line = line.replace(/^(From:\s*.+?)@[\w\.-]+\.\w+/m, `$1@${domainReplacement}`);
                        } else {
                            line = line.replace(/^(From:\s*.+?)@[\w\.-]+\.\w+/m, '$1@[P_FROM]');
                        }
                    }
                    
                    if (header.name === 'Message-ID' && messageIdTag) {
                        line = line.replace(/Message-ID:\s*<(.+?)(@.+?)>/g, `Message-ID: <$1${messageIdTag}$2>`);
                    }
                    
                    if (header.name === 'Date' && replaceDate) {
                        line = 'Date: [DATE]';
                    }
                    
                    if (header.name === 'To' && replaceTo) {
                        line = 'To: [*to]';
                    }
                    
                    otherHeaders.push(line);
                }
            }
            
            // Build final output
            let result = [];
            result = result.concat(receivedHeaders);
            
            if (senderHeader) {
                result.push(senderHeader);
            }
            
            if (customHeaders) {
                customHeaders.split('\n')
                    .filter(line => line.trim())
                    .forEach(line => result.push(line));
            }
            
            result = result.concat(otherHeaders);
            
            if (addCc && !result.some(line => line.startsWith('Cc:'))) {
                const subjectIndex = result.findIndex(line => line.startsWith('Subject:'));
                if (subjectIndex !== -1) {
                    result.splice(subjectIndex, 0, 'Cc: [*to]');
                }
            }
            
            let processed = result.join('\n').trim();
            
            if (extractBody && bodyContent.trim()) {
                processed += '\n\n' + bodyContent.trim();
            }
            
            return processed;
        }

        // Update Stats
        function updateStats() {
            document.getElementById('totalProcessed').textContent = processedCount;
            document.getElementById('totalFiles').textContent = uploadedFilesContent.length;
            
            const avgTime = processedCount > 0 ? (totalProcessingTime / processedCount).toFixed(2) : 0;
            document.getElementById('processingTime').textContent = avgTime + 's';
        }

        // Download Output
        async function downloadOutput() {
            const output = document.getElementById('outputHeaders').value;
            if (!output) {
                showNotification('Nothing to download!', 'warning');
                return;
            }
            
            const format = document.getElementById('exportFormat').value;
            
            // Handle different formats
            if (format === 'sep') {
                downloadWithSeparator();
            } else if (format === 'zip') {
                await downloadAsZip();
            } else if (format === 'json') {
                downloadAsJson();
            } else if (format === 'csv') {
                downloadAsCsv();
            } else {
                downloadAsTxt();
            }
        }

        // Download as single TXT file
        function downloadAsTxt() {
            const output = document.getElementById('outputHeaders').value;
            const blob = new Blob([output], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'headers.txt';
            a.click();
            URL.revokeObjectURL(url);
            showNotification('Download started! 📥', 'success');
        }

        // Download merged with __SEP__ separator
        function downloadWithSeparator() {
            if (processedFilesData.length === 0) {
                showNotification('No processed data available!', 'warning');
                return;
            }

            let mergedContent = [];
            for (let i = 0; i < processedFilesData.length; i++) {
                mergedContent.push(processedFilesData[i].content);
            }
            
            // Join with __SEP__
            const content = mergedContent.join('\n__SEP__\n');
            
            const blob = new Blob([content], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'headers_merged_sep.txt';
            a.click();
            URL.revokeObjectURL(url);
            showNotification('Downloaded merged file with __SEP__! 📥', 'success');
        }

        // Download as ZIP with separate files
        async function downloadAsZip() {
            if (typeof JSZip === 'undefined') {
                showNotification('JSZip library not loaded! Please refresh the page.', 'error');
                return;
            }

            if (processedFilesData.length === 0) {
                showNotification('No processed data available!', 'warning');
                return;
            }

            try {
                const zip = new JSZip();
                
                // Add each file to ZIP
                for (let fileData of processedFilesData) {
                    // Clean filename and add .txt extension if needed
                    let filename = fileData.name;
                    if (!filename.endsWith('.txt') && !filename.endsWith('.eml')) {
                        filename = filename.replace(/\.[^/.]+$/, '') + '.txt';
                    }
                    
                    zip.file(filename, fileData.content);
                }
                
                // Generate ZIP file
                const blob = await zip.generateAsync({ type: 'blob' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = 'processed_headers.zip';
                a.click();
                URL.revokeObjectURL(url);
                
                showNotification(`Downloaded ZIP with ${processedFilesData.length} file(s)! 📦`, 'success');
            } catch (error) {
                console.error('Error creating ZIP:', error);
                showNotification('Error creating ZIP file!', 'error');
            }
        }

        // Download as JSON
        function downloadAsJson() {
            const output = document.getElementById('outputHeaders').value;
            const content = JSON.stringify({ headers: output.split('\n') }, null, 2);
            const blob = new Blob([content], { type: 'application/json' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'headers.json';
            a.click();
            URL.revokeObjectURL(url);
            showNotification('Downloaded JSON! 📥', 'success');
        }

        // Download as CSV
        function downloadAsCsv() {
            const output = document.getElementById('outputHeaders').value;
            const content = 'Header\n' + output.split('\n').map(line => `"${line}"`).join('\n');
            const blob = new Blob([content], { type: 'text/csv' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'headers.csv';
            a.click();
            URL.revokeObjectURL(url);
            showNotification('Downloaded CSV! 📥', 'success');
        }

        // Copy to Clipboard
        function copyToClipboard() {
            const output = document.getElementById('outputHeaders').value;
            if (!output) {
                showNotification('Nothing to copy!', 'warning');
                return;
            }
            
            navigator.clipboard.writeText(output).then(() => {
                showNotification('Copied to clipboard! 📋', 'success');
            });
        }

        // Show Comparison
        function showComparison() {
            const input = document.getElementById('inputHeaders').value;
            const output = document.getElementById('outputHeaders').value;
            
            if (!output) {
                showNotification('Process headers first to compare!', 'warning');
                return;
            }
            
            document.getElementById('comparisonSection').classList.remove('hidden');
            document.getElementById('comparisonBefore').textContent = input || 'Uploaded files';
            document.getElementById('comparisonAfter').textContent = output;
            
            document.getElementById('comparisonSection').scrollIntoView({ behavior: 'smooth' });
        }

        // Clear All
        function clearAll() {
            document.getElementById('inputHeaders').value = '';
            document.getElementById('outputHeaders').value = '';
            uploadedFiles = [];
            uploadedFilesContent = [];
            processedFilesData = [];
            updateFileList();
            originalHeaders = '';
            processedHeaders = '';
            document.getElementById('comparisonSection').classList.add('hidden');
            showNotification('Cleared all data!', 'success');
        }

        // Show Notification
        function showNotification(message, type = 'success') {
            const notification = document.createElement('div');
            notification.className = `notification ${type}`;
            notification.innerHTML = `
                <strong>${type === 'success' ? '✅' : type === 'warning' ? '⚠️' : '❌'} ${type.toUpperCase()}</strong>
                <p style="margin-top: 0.5rem;">${message}</p>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.animation = 'slideInRight 0.3s ease-out reverse';
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            showNotification('CMH-1 Pro FIXED loaded successfully! 🚀', 'success');
        });
    </script>
</body>
</html>
